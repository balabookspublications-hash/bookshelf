# Local deployment state

`hosting.json` associates a checkout with one Sites deployment project. It is
intentionally gitignored because forks and contributors should create or select
their own project rather than inherit the maintainer's project ID.

Do not invent or hand-edit Sites IDs. Let the Sites integration create
`hosting.json`, then keep it local.
